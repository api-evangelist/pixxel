openapi: 3.2.0
info:
  description: Swagger doc for Stargate
  title: stargate External API
  contact:
    name: Team-Identity
    email: identity@pixxel.co.in
  version: v0.1.0
servers:
- url: https://api.pixxel.space
tags:
- name: External
paths:
  /ping:
    get:
      description: Returns a pong message.
      tags:
      - External
      summary: Ping Server
      responses:
        '200':
          description: Successful ping
          content:
            application/json:
              schema:
                type: string
      operationId: getPing
      x-operation-id-source: derived
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      name: Authorization
      in: header
